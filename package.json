{
  "name": "welcome-page",
  "version": "1.0.0",
  "description": "A welcome page built with React and TypeScript",
  "main": "index.js",
  "scripts": {
    "prestart": "npm run sync:videos && npm run sync:slides",
    "prebuild": "npm run sync:videos && npm run sync:slides",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "sync:videos": "xcopy videos public\\videos /E /I /Y >NUL 2>&1 || exit /b 0",
    "sync:slides": "xcopy ..\\stacked-slides-app\\dist public\\videos\\stacked-slides-app /E /I /Y >NUL 2>&1 || exit /b 0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "typescript": "^4.9.5"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
